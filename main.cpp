// Командный проект. Группа ПИ-51.
// Команда: Мишустин (в. 20), Майстренко (в. 22, техлид).
#include <iostream>
// === БЛОК ПОДКЛЮЧЕНИЙ: каждый участник добавляет свой заголовочный файл ===
#include "Maistrenko.h"
// === КОНЕЦ БЛОКА ПОДКЛЮЧЕНИЙ ===
using namespace std;
int main() {
	int choice;
	do {
		cout << "\n=== Командный проект: сборник расчётов ===\n";
		// === БЛОК МЕНЮ: каждый участник добавляет свои пункты ===
		// === КОНЕЦ БЛОКА МЕНЮ ===
		cout << "0. Выход\n";
		cout << "Выберите пункт: ";
		cin >> choice;
		switch (choice) {
		case 1:
			cout << "Введите начальную скорость (v0), ускорение (a) и время (t): ";
			cin >> v0 >> a >> t;
			cout << "Конечная скорость = " << finalSpeed(v0, a, t) << "Метров в секунду" << "\n";
			break;
		case 2:
			cout << "Введите начальную скорость (v0), ускорение (a) и время (t): ";
			cin >> v0 >> a >> t;
			cout << "Перемещение = " << displacement(v0, a, t) << "Метров" << "\n";
			break;
		case 0:
			cout << "Работа завершена.\n";
			break;
		default:
			cout << "Такого пункта нет.\n";
		}
	} while (choice != 0);
	return 0;
}